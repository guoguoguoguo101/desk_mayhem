# 战斗网络架构（2026-09-25）

本页描述当前实现，替代此前“局部受击预演”的路线。范围是菜单中的**加入独立测试战斗服**：两名玩家、两只稻草人、固定雨伞与锅。旧办公室单人模式、ENet 玩家开房大厅仍保留原路径；本次没有重写咖啡、椅子、AI、多人匹配或公网鉴权。

## 唯一战斗核心

- `combat/combat_world.gd`：完整房间模拟，60 Hz 固定步长。服务端和客户端预测调用同一个 `step(commands)`。
- `server/battle_server.gd`：UDP、会话令牌、席位、输入校验、定时快照、可靠消息；不再自行实现技能伤害和运动。
- `client/world_prediction.gd`：权威世界、输入历史、整世界恢复与重放、误差统计。
- `client/battle_client_session.gd`：连接、采集输入、呈现模拟结果和反馈。已删除 TargetPrediction、self_motions、pending_hits、独立本地飞锅等特例。
- `combat/arena_motion.gd`：共享地形胶囊运动、浮空、落地、墙反弹、闪现及动态胶囊阻挡。
- `combat/battle_rules.gd`、`float_rules.gd`、`combat/battle_pot.gd`：原技能数值、浮空规则、飞锅运动。此次没有为联网另调高度、重力、冷却。

每个模拟器通过独立 SubViewport/World3D 拥有自己的物理空间，客户端模型不会参与模拟碰撞。地形使用 Godot 静态碰撞；玩家与稻草人的阻挡直接按世界状态做扫掠胶囊检测。后者避免多 Tick 重放时 PhysicsServer 动态碰撞缓存仍指向旧位置。玩家之间尚无实体推挤。

## Tick 的执行顺序

1. Tick 加一，处理到期的回合重置。
2. 输入按实体 ID、输入序号排序；检查回合与 life；更新控制，按 attack_seq 去重动作。
3. 按 Tick 起始的动态阻挡位置推进实体移动、浮空、墙/地面碰撞；处理复活、输入超时、预输入释放。
4. 接受这一 Tick 的新动作；计算朝向、冷却、取消窗口、空中扣锅选择。
5. 推进动作时间轴和全部飞锅，收集候选命中。
6. 根据命中前的状态批量结算；伤害相加、硬直取最长、运动按既有优先级选择。双方同 Tick 上勾可以同时击飞，互相致死为平局。
7. 完成动作结束、死亡/比分、事件日志，得到本 Tick 结束后的完整世界。

战斗计时全部由 Tick 推导；毫秒形式的冷却截止值也是模拟时间，不读电脑时钟。现实时间仅用于网络超时、重试、RTT 和显示平滑。

取消了旧的“把目标暂时搬回历史位置，再用当前浮空状态结算”的局部命中回溯。所有命中几何、保护、浮空和伤害使用同一个世界 Tick。迟到输入不能让服务器倒流已经确认的对局，也不能通过积累输入加速移动。

## 输入与时间确认

每帧包括 entity_id、seq、目标 tick、round_id、life、move、aim、actions；服务器从连接推导 entity_id，不信任客户端身份。每个动作带独立 attack_seq。

客户端按 RTT 为操作预留少量传输提前量，本地下一物理 Tick 即执行（通常不超过 16.7 ms），不等服务端批准。提前量有上限，掉线时最多向最新快照外推 18 Tick，随后停止盲目模拟。节奏调整会约束过大的提前量，不无限向前漂移。

服务器缓存合法的未来输入，到期每 Tick 仅推进一次；迟到动作在当前 Tick 按同样规则验证。方向必须有限且归一范围合法，序号/未来时间/队列有界。移动包冗余携带最近八帧；含动作的同一帧另经可靠通道重试。两路都进入相同去重入口，不存在独立“出招批准后再动”的流程。

快照确认两种不同事实：

- input_seq：已经采用或覆盖的移动控制。
- command_receipts：已经消费的 attack_seq，**不代表命中成功**。动作可能被缓冲、拒绝或打空，结果已经包含在权威世界中。

快照也携带服务器已经收到、等待未来 Tick 执行的输入，客户端可重放这些已知的远端动作。尚未收到的对手输入不可能提前知道，仍会产生正常的权威纠偏。

## 完整世界恢复

快照包含所有玩家/NPC 的位置、速度、朝向、血量、life、死亡、浮空、倒地、保护、硬直、动作起始 Tick/游标/锁定方向/序号、冷却、预输入、连拳阶段、复活时间、比分；以及全部飞锅的位置、方向、年龄、去回程、召回、每程已命中集合、所有 ID 计数器、回合状态与有界事件日志。

恢复时同时替换所有实体、碰撞胶囊位置和飞锅，不只恢复本地玩家或某一个受击目标。移除快照中没有的实体，清理旧 life/回合的输入。然后逐 Tick 重放仍未被确认的完整输入：移动、技能起手、目标选择、命中、浮空、飞锅、落地一起重新计算。迟到但尚未被消费的命令放到恢复点后继续等待，不重复添加已确认冲量。

飞锅的命中集合和历史世界必须深复制；恢复后的模拟不可修改历史快照。保存有界世界历史与输入历史，长期缺包不会无限增长。

## 模拟和显示分离

模型位置只跟随预测世界进行渲染平滑，不能反向写入战斗核心，也不再切回落后 100 ms 的目标插值轨迹。攻击动画阶段从模拟 Tick 推导；受击中断和纠偏后能同步取消。角色脚本的 external_combat_view 分支只画模型，不运行旧命中、复活或位置纠偏。

预测世界会计算完整的血量/死亡等状态，以便正确重放后续交互；**对用户显示的血量、死亡与比分取权威世界**。预测结果不上传为事实。受击反馈用回合、实体、生命、attack_seq、招式/飞锅段等键去重，重放和确认不会重复播放。

30° 镜头修正规则不变：站立转镜头不转身体；出招时 ≤30° 用镜头方向，否则用身体方向；动作锁定方向，旋伞动画独立。

## 传输与运维边界

协议版本 3，旧客户端/服务器不兼容，必须一起更新。全世界快照以不含对象的 Variant 序列化并压缩；解码禁止对象构造，限制解压大小。游戏快照拆成 700 字符数据片，乱序重组；部分丢失的旧快照无需重发，后续完整快照接替。重组队列有界。协议测试可请求 debug_roster 明文观测行，此模式只用于测试，不是游戏默认传输。

出招帧、死亡、复活、回合消息可靠重试；表现事件可直接发送，完整快照的最近 64 条事件日志补漏。快照只在完整收到后、物理 Tick 边界原子应用。网络 ACK 不等于命中确认。

日志 BATTLE_SYNC 含 tick、ahead、rtt、pending、same_tick_error、correction、rebases、replay_steps。初次入场的位置差不代表纠偏故障；同 Tick 状态差、迟到重排和完整世界重放成本才是排查依据。

仍不是正式公网服务：没有账号鉴权、DDoS 防护、匹配、房间编排、多人数压力验收。固定浮点/Godot 物理不能承诺不同平台逐位确定性，更不能承诺未知对手操作有 99% 预测命中率。测试与启动见 BATTLE_TESTING.md。
