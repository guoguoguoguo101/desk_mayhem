# 《工位失控》客户端与战斗服务器路线

> 设计决策，2026-09-23。本文描述**后续应遵循的目标架构与迁移步骤**，不表示当前版本已经有独立服务器。当前可玩内容见 [PROJECT_CONTEXT.md](PROJECT_CONTEXT.md)。实现细节改变时同步更新本文。

## 决策

战斗客户端和战斗服务器继续使用 **同一个 Godot 项目中的共享战斗逻辑**。服务器以独立进程运行 Godot `--headless`，只加载地图碰撞、角色碰撞体、战斗状态、道具判定和对局规则。客户端加载模型、动画、音效、粒子、镜头与 HUD。服务器判定战斗事实，客户端负责即时操作手感和画面表现。

**暂不以 Go 重写战斗逻辑。**将来若需要账号、好友、匹配、战绩数据库或战斗进程调度，可再引入 Go 等外围服务；它不参与每帧的击飞与碰撞计算。

Godot 官方支持使用 `--headless` 运行，也支持专用服务器导出以剔除不需要的视觉资源。需要注意：**`--headless` 只是不渲染，不会自动移除场景里的模型、UI 节点或纹理引用**。因此需要单独的服务器入口场景与导出配置。参见 [Godot 专用服务器文档](https://docs.godotengine.org/en/stable/tutorials/export/exporting_for_dedicated_servers.html)。

## 当前代码的起点与缺口

当前 `network_manager.gd` 是**玩家电脑开房的局域网 listen server**，不是独立服务器。已有 ENet、客户端输入消息、约 33 ms 一次的主机状态快照、远端角色插值、命中确认及双进程测试框架。这些可以保留思路，但要调整职责。

| 现状 | 迁移要求 |
| --- | --- |
| `NetworkManager` 在 `_ready()` 里取 `../Player` 和 `../CombatFeedback`，构建大厅、比分 UI，并把主机本地玩家当作参赛者 | 拆分 `ClientSession` 与 `ServerSession`；服务器允许 **零本地玩家**，每个参赛者由连接的 peer 建立 |
| `player.gd` 同时处理输入、角色运动、命中、受伤、建模、动画、准星和联机纠偏 | 把纯战斗状态/判定抽到共享代码；输入适配和画面表现各留在客户端或服务器外层 |
| `dummy.gd` 与 `player.gd` 各写一套类似的血量、浮空、倒地计算 | 合并共同的战斗规则，角色差异只保留配置和 AI/控制器 |
| 客户端可以经 `receive_hit`、`relay_dummy_hit`、`register_kill` 等路径推动受击或计分；`receive_carry` 可改玩家位置 | 服务器重算命中、伤害、携带、倒地和比分；客户端只能发操作意图，不能提交结果 |
| `duel_hall.gd` 的 `_ready()` 会创建贴图、几何体和装饰 | 服务器只加载简化庭院碰撞及出生点；美术场景留客户端 |
| `confirm_hit`、`world_state`、`receive_state` 等多条状态路径并存 | 明确单一权威状态流与事件流，废弃重复或相互矛盾的同步路径 |

其中最先要解决的是**战斗事实的唯一来源**。Godot 的高层多人 API 不会自动防止客户端伪造位置、命中或计分；官方建议服务端判定关键游戏状态并校验客户端输入，见 [Godot 高层多人文档](https://docs.godotengine.org/en/stable/tutorials/networking/high_level_multiplayer.html)。

## 目标运行结构

```text
同一份 Godot 项目
├─ shared/                 战斗规则、数据类型、地图碰撞定义
├─ client/                 输入、镜头、模型、动画、音效、HUD、预测与插值
└─ server/                 Headless 入口、房间、权威模拟、快照与事件

客户端 A ── InputFrame ─┐
客户端 B ── InputFrame ─┼─→ Godot 战斗服 ─→ StateSnapshot / CombatEvent
客户端 C/D（未来） ─────┘
```

服务器场景只应包含 `MatchServer`、地图静态碰撞、出生点、玩家 capsule/hitbox/hurtbox、道具碰撞、地面区域及规则节点。动画骨骼、尾巴摆动、粒子和 ragdoll 不参与权威模拟。服务器同步的是“谁被什么击中、处于什么状态、正以什么速度运动”；客户端根据事件播放动作和特效。

客户端画面不必复刻服务器的所有刚体细节。**凡会影响伤害、站位、阻挡或胜负的碰撞必须在服务器上存在并决定结果**；纯装饰碎片和 ragdoll 四肢只在客户端模拟。否则双方看到的“墙”和判定用的“墙”会不一致。

## 共享 CombatCore 的边界

建议先抽出以下数据与纯规则，再迁移网络：

| 模块 | 输入 | 输出 |
| --- | --- | --- |
| `AttackDefinition` / `HitData` | 攻击 ID、来源、伤害、作用范围、基础击飞、硬直及特殊标志 | 一次候选命中 |
| `HitDetector` | 攻击形状、角色/道具碰撞状态、物理 tick | 候选目标列表；**服务器再次校验**距离、角度、冷却与阵营 |
| `CombatResolver` | `HitData`、目标 `CombatState`、碰撞法线 | 新的 HP、速度、浮空/倒地状态及 `CombatEvent` |
| `FloatRules` / `WallRules` | 高度、速度、连续追击负荷、墙法线 | 停顿、下落、墙反弹和落地保护 |
| `CombatState` | 权威数值 | 可序列化的战斗状态 |

现阶段真实数值是 **HP**。此前讨论过“压力值取代血条并影响击飞距离”，但当前尚未实现；若要改成压力系统，先在单机练习场验证规则，再由同一套 `CombatResolver` 同时供客户端预测与服务器判定，避免文档先把构想写成既有功能。

推荐的依赖方向：

```text
输入 / AI → AttackIntent → CombatCore → CombatState + CombatEvent
                                    ├→ 服务器保存权威结果
                                    └→ 客户端播放动画 / 音效 / 特效
```

`CombatCore` 不能依赖 HUD、Camera3D、粒子或“当前本地玩家”。服务器与客户端使用同一份攻击数值配置，避免两套手感参数漂移。**共享规则不等于让客户端决定命中**：客户端跑同一规则仅用于预测，服务器的结果始终覆盖预测。

## 网络消息与同步规则

| 方向 | 消息 | 必要内容 | 原则 |
| --- | --- | --- | --- |
| 客户端 → 服务器 | `InputFrame` | peer、输入序号/物理 tick、移动方向、视角/瞄准方向、跳/闪/攻击/武器选择等按钮状态 | 只表达意图；限制频率，验证序号、方向范围与动作是否合法 |
| 服务器 → 客户端 | `StateSnapshot` | 服务器 tick、每名玩家位置/速度/朝向、HP、战斗状态、装备、冷却或可推导计时器、浮空负荷 | 是持续状态的权威版本；可按需压缩和降低发送频率 |
| 服务器 → 客户端 | `CombatEvent` | 事件 ID/tick、攻击者、受击者、攻击 ID、命中点/法线、伤害、关键状态变化 | 驱动命中特效、声音和一次性动画；去重，不用同步骨骼 |
| 服务器 → 客户端 | `MatchEvent` | 加入/离开、倒地、复活、计分、胜负 | 只由服务器产生 |

服务器使用固定物理 tick；先以项目现有物理设置运行，网络快照从目前约 **30 次/秒** 的发送节奏起测，再根据带宽与手感测量调整。连续的输入与状态可用有序非可靠通道，加入、装备变化及不可丢的对局事件走可靠通道。RPC 双端的节点路径、权限与序列化结构必须保持一致；Godot 对 RPC 节点路径有此要求，见 [官方说明](https://docs.godotengine.org/en/stable/tutorials/networking/high_level_multiplayer.html)。

### 本地手感

1. 本机角色先预测移动和出手动作，并保存带序号的输入历史。
2. 收到服务器快照后，以服务器状态为准重放尚未确认的输入；小误差平滑修正，较大误差直接纠正。
3. 其他玩家在两个已收到的快照之间插值显示。击飞、撞墙、倒地等关键事件以服务器确认的 `CombatEvent` 和状态为准。
4. 浮空长连招对位置误差敏感，先确保**服务器命中与客户端画面一致**，再扩大预测范围；不要一开始预测所有连招结果或完整 ragdoll。

## 分阶段实施与验收

### 阶段 1：保住当前试玩手感

锁定一组基线连招，例如“拳 → 上勾/雨伞挑飞 → 空中补拳 → 踢 → 撞墙 → 落地”。录制或记录单机中位置、HP、浮空高度和时长。修正文档与旧 README 中不符的操作。此阶段不需要 Go。

**验收**：单机训练场仍可完整操作，连招前后数值稳定，空中攻击不会因帧率变化明显失真。

### 阶段 2：先抽共享战斗规则

在现有场景内抽 `CombatState`、`HitData`、`CombatResolver`、浮空/墙/倒地规则。将 `player.gd` 与 `dummy.gd` 的伤害和状态更新收敛到同一路径；视觉反馈订阅战斗事件。此时保留现有局域网开房，避免同时重写全部内容。

**验收**：相同初始状态与攻击输入产生相同结果；训练稻草人与玩家走共享规则；HUD、音效关闭时仍能计算完整战斗。

### 阶段 3：独立 Godot Headless 战斗服

增加 `server_main.tscn` / `server_main.gd`，由命令行或 dedicated server feature 启动；原 `NetworkManager` 拆成客户端会话和服务端房间。服务器按连接创建战斗角色，**不要求主机兼任玩家**。先支持两客户端连同一服务器，再将所有命中、血量、稻草人、道具和比分改为服务端权威。服务器导出时剔除不需要的纹理和美术资源。

**验收**：单机同机的“1 服务器 + 2 客户端”测试通过；客户端发伪造位置、HP、命中或计分不能改变权威结果；无显卡/无窗口环境能启动服务器。随后在两台电脑上测同一局域网。

### 阶段 4：预测、纠偏与稳定性

加入输入 tick/序号确认、客户端重放、远端插值与事件去重；模拟延迟和丢包，覆盖跳跃、浮空、冲椅、撞墙及倒地复活。记录服务器日志中的 tick、输入序号、攻击 ID 和状态差异，便于复现。

**验收**：在目标网络条件下，角色能顺畅移动且攻击结果最终一致；断线、重连或退出不会留下幽灵玩家或错误比分。具体可接受的延迟阈值通过试玩确定。

### 阶段 5：外围服务（有实际需求时）

再决定是否增加 Go 服务处理登录、好友、匹配、房间分配和战绩存储。Go 只向 Godot 战斗服发房间配置、授权和结算请求；战斗 tick 仍由 Godot 决定。

## 当前近期任务顺序

1. 以现有局域网 1v1 做两机实测，记下同步错位和操作延迟。
2. 将 HP/浮空/硬直/倒地与命中描述从 `player.gd`、`dummy.gd` 抽成共享规则；保留现有攻击动作。
3. 统一服务器命中与计分入口，先停用客户端直接上报最终战斗结果的路径。
4. 再建独立的无画面服务器场景；不要把现有 `office_demo.tscn` 简单加 `--headless` 就当成完成拆分。

这份路线的重点是：**先让一套战斗规则在单机中可测，再让独立服务器成为唯一裁判**。公网匹配、复杂部署和 Go 后端都排在核心手感与权威同步之后。
