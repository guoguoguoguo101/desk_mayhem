# 《工位失控》项目现状

> 接手项目时先读本文件。内容根据 `main` 分支提交 `639b749` 的代码整理，核对日期为 2026-09-23。它记录**当前已经实现的原型**；新改动落地后应同步更新。具体行为以代码和实际运行结果为准。

客户端与独立服务器的后续设计见 [NETWORK_ARCHITECTURE.md](NETWORK_ARCHITECTURE.md)；该文是目标路线，不代表当前已实现。

## 一句话介绍

Godot 4 的第三人称 3D 乱斗原型：玩家控制一只卡通小狗，在单人练习场打稻草人，或通过局域网进入双人比武大厅。当前重点是拳脚、办公室道具、浮空连招与击飞手感，尚无完整对局目标或正式美术资产。

## 最近两笔提交

这两笔提交的 Git 时间均为 **2026-09-22（北京时间）**，并非 9 月 23 日当天的新提交。

| 提交 | 主要内容 |
| --- | --- |
| `c9b8166` | 新增局域网 1v1 比武大厅、双武器槽和技能 HUD；加入锅、冲椅、咖啡等战斗动作；新增独立的中式庭院场景、Poly Haven 贴图、稻草人/玩家受击与联机同步逻辑。 |
| `639b749` | 调整浮空：升至出拳高度后短暂停留、随后较慢下落；目标浮空时左键改为补拳；补充联机动作/状态同步和双进程自动测试脚本。 |

## 现在如何进入游戏

用 Godot 打开 `project.godot`，运行主场景 `office_demo.tscn`。启动后先到菜单：

- **单人练习**：进入办公室练习场，打原场景的 16 个稻草人。
- **开设比武大厅**：本机作为主机，在局域网端口 `24680` 开房；画面切换到中式庭院，并有两只练习稻草人。
- **加入比武大厅**：另一台电脑填入主机的局域网 IP 后加入。当前菜单和人数上限按 **1v1** 运作。
- **F8**：从练习或对战返回菜单。大厅显示本机 IP、对手血量和击杀榜。

`network_manager.gd` 中已有 2v2、四人乱战的常量和槽位逻辑，但当前菜单只开放 1v1；不要把多人模式写成已可玩的功能。仓库有双进程联机测试，尚不能代替两台电脑的实战验证。

## 当前操作

| 操作 | 效果 |
| --- | --- |
| WASD / 方向键 | 按镜头方向移动 |
| 鼠标移动 | 转动跟随镜头；角色移动和技能不会强行转镜头 |
| 鼠标滚轮 / R | 调整镜头距离 / 镜头回正 |
| Esc / 点击画面 | 释放 / 重新捕获鼠标 |
| Shift / 空格 / Ctrl | 向前短闪现 / 跳跃 / 下蹲 |
| 左键 | 轻拳；命中后可接连拳、上勾拳；目标浮空时变为补拳 |
| 右键 | 前踢；命中浮空目标会踢得更远 |
| Tab、1–4 | 选中 A/B 武器槽；将雨伞、咖啡杯、锅、办公椅装入该槽。开局为雨伞＋锅 |
| Q、E / F、C | 分别使用武器 A / 武器 B 的两个动作，具体名称和冷却显示在 HUD |

### 四件武器的两个动作

| 武器 | 第一个动作 | 第二个动作 |
| --- | --- | --- |
| 雨伞 | 前冲；冲锋结束后有再次按键挑飞的窗口 | 旋伞，打击周围目标 |
| 咖啡杯 | 投掷咖啡，落地生成咖啡滩 | 喝咖啡，短时加速 |
| 锅 | 回旋锅：沿角色面向飞出，再按快速召回 | 扣锅；目标浮空时触发较快的空中扣锅 |
| 办公椅 | 放出向前冲的椅子，可卷住目标并在撞墙时抛出 | 坐上 / 离开椅子 |

目前**文件夹不是可装备的玩家武器**。`thrown_item.gd` 仍保留文件夹投射物类型，稻草人的攻击代码也使用投射物；旧 README 中“左键扔文件夹、右键扔咖啡”的说明已过时。

## 战斗规则和手感

- 玩家按镜头方向移动，攻击主要沿角色面向/当前瞄准方向释放。投掷咖啡时，镜头与角色朝向相差不超过约 **30°** 就取屏幕中心的水平瞄准方向；偏差更大时退回角色面向方向。回旋锅沿角色面向飞出。装备咖啡或锅时出现中心准星；咖啡另有预测落点圈。
- 左键轻拳需要命中才能继续连拳、上勾拳。上勾、雨伞挑飞、空中补拳、踢击和扣锅可以组合成空中追击。连击数和冷却显示在 HUD。
- `float_rules.gd` 负责浮空重力、追击抬升、踢墙反弹以及落地后的短暂倒地保护。三拳上勾和雨伞挑飞采用较强的初始向上速度，迅速把目标打到明显的高度；较强的固定重力使其到顶点后自然加速下落，避免缓慢上飘。没有顶点停顿、浮空条或固定时长的强制落地。
- 同一次浮空中，第一记空中补拳会轻微托住目标；后续补拳只造成伤害并调整横向方向，不重置下坠速度。旋伞每次命中浮空目标都可重新给予向上速度，甩锅命中也可小幅抬升；不设每轮浮空只能续连一次的限制。目标若没有被再次命中抬升，就会依照重力落地。
- 角色和稻草人都有血量、受击、倒地和复活状态。单人练习时玩家上限为 **1000 HP**；联机比武时玩家为 **400 HP**；稻草人为 **2000 HP**。普通办公室稻草人约 5 秒复活，大厅稻草人约 3 秒复活。
- 打击反馈包含音效、停顿、闪色、镜头轻震、碎片、连击字样等。角色和家具仍主要由 Godot 基础几何体构成，属于灰盒美术。

回旋锅每人同时最多一口，飞出约 8 米后自动返回；飞行中再次按原技能键会立即高速召回。每个目标去程、回程各最多命中一次：去程推开地面目标、轻抬浮空目标；回程朝玩家位置拉回，浮空目标仅减缓下坠，保留已有上升速度。去程遇墙提前折返，回程被墙阻挡则消散，命中不会穿墙。去程金色、回程青色，主动召回有脉冲和加长拖尾；闪光使用 Kenney CC0 粒子素材，来源及许可在 `assets/kenney_particles/`。局域网主机判定命中，客户端播放飞行与召回效果。

可以先试：**三拳挑飞 → 回旋锅 → 再按快速召回 → 旋伞 → 空中扣锅**；或者 **雨伞冲锋 → 再按挑飞 → 空中扣锅**。武器在 A/B 槽的位置会改变按键，不改变动作本身。默认锅在 B 槽：F 回旋锅/召回，C 扣锅。

## 地图与对局范围

- **办公室练习场**：`office_demo.tscn` 中的 110×80 地面、围墙、办公桌、会议区、茶水区、箱子与 16 个稻草人；由 `make_scene.py` 生成。
- **比武大厅**：`duel_hall.gd` 运行时搭建约 56×44 的中式庭院，切换时隐藏办公室物件；使用 `assets/polyhaven/` 下的贴图。比武大厅有 2 个可被击打的稻草人。
- **联机**：`network_manager.gd` 使用 ENet；主机负责主要的动作/命中判定和状态快照，客户端发送输入并显示同步结果。当前是局域网 IP 加入模式，没有公网房间服务、匹配、账号或完整结算规则。击杀榜统计玩家与大厅稻草人的击倒。

## 关键文件

| 文件 | 职责 |
| --- | --- |
| `project.godot`、`office_demo.tscn`、`make_scene.py` | 项目入口、主场景及场景生成器 |
| `player.gd` | 玩家移动、输入、武器、连招、瞄准、受击与联机状态 |
| `dummy.gd`、`float_rules.gd` | 稻草人行为及玩家/稻草人共用的浮空规则 |
| `network_manager.gd`、`pvp_net_test.gd` | 菜单、局域网房间、同步、计分与双进程测试 |
| `duel_hall.gd`、`rushing_chair.gd` | 比武大厅与冲椅物体 |
| `thrown_item.gd`、`coffee_puddle.gd` | 投射物和咖啡滩 |
| `returning_pot.gd`、`tools/returning_pot_test.gd` | 回旋锅飞行、两段命中与特效，以及实际场景回归测试 |
| `follow_camera.gd`、`combat_hud.gd`、`combat_feedback.gd` | 镜头、准星/技能栏及打击反馈 |

## 开发状态与注意事项

1. 这是可试玩原型，当前主要循环是练习连招或进行 1v1 比武；先前讨论的“抢工牌、搜装、逃脱、老板 Boss、可破坏办公室”仍是方向，**尚未作为完整玩法落地**。
2. 新代码把“武器”做成随时用数字键替换的两个槽位，还没有地图拾取、库存或资源限制；这与早期“从办公室捡起任何物品”的构想不同。
3. 最新场景启动需要先让 Godot 编辑器导入 `assets/polyhaven/` 的 JPG。直接在全新检出的目录里运行无界面的游戏，可能先报贴图资源未导入；打开一次编辑器即可完成导入。
4. 本次核对使用 Godot 4.7.2：编辑器导入成功；随后无界面启动仍输出两条渲染材质参数警告。尚未据此判断实际画面是否异常，也尚未在本次核对中跑两台电脑的联机试玩。
